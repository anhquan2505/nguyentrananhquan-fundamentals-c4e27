-nested list is as the same as nested conditionals, which is described when a list is within another list
for example:
- you can give a list 'in' a list like this:
v=['a','b',['c','d']]
-or :
a=[1,2]
v=[a,'b']


-you can give both integer and string in a same list together.