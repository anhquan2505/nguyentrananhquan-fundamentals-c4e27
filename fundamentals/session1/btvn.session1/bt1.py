- Valuable is a name that refers to a value 

And the value has some popular types such as strings, integer or float-point numbers
Strings could be identified because they are enclosed in quotation marks.
how to check:
type_x = type(x)
type(object_or_name, bases, dict)
param object_or_name

type(object) -> the object's type
type(name, bases, dict) -> a new type



- If you give a variable an illegal name, you get a syntax error.
the name will be illegal if it has one of three things below:
 + does not begin with a letter
 + contains an illegal character, such as the dollar sign or a pencentage (%) sign,...
 + it is keywords which define the language’s rules and structure, and they cannot be used as variable names.
For example: 25python, percent%, lambda,...
